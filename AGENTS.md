# open-workstation-mcp Agent Instructions

This repo is public-safe by design. Do not add private workstation paths,
account data, secrets, screenshots, browser profiles, or copied private repo
implementation details. Prefer synthetic fixtures and dry-run examples.

Run `make ci` and `gitleaks detect --source . --redact` before publishing.
